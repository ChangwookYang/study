import java.util.*;

class Solution {
    public int lengthOfLastWord(String s) {
        String[] str = s.split(" ");
        int length = str.length;
        if(length < 1) return s.replaceAll(" ", "").length();
        return str[length-1].length();
    }
}
